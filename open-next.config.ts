// @ts-nocheck
// OpenNext config for Cloudflare Pages
// Package is installed during Cloudflare build, not locally
import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig({});
