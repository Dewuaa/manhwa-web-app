// OpenNext configuration for Cloudflare
// Simple config without imports - OpenNext will handle the rest

/** @type {import('@opennextjs/cloudflare').OpenNextConfig} */
const config = {};

module.exports = config;
